# CyberShield — Python 3.11+ (tested on 3.13 / Windows)
# scikit-learn must match the version used to train the bundled .pkl models (1.5.x).
# On Python 3.13 use exactly 1.5.2 (has win_amd64 wheels). Do NOT use 1.5.1 on 3.13 — pip falls back to source and needs MSVC.

flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
pandas>=2.0.0
numpy>=1.26.0
scikit-learn==1.5.2
bcrypt>=4.0.0
psutil>=5.9.0
requests>=2.28.0
